{
  "name": "hookbuildrun",
  "version": "1.0.0",
  "description": "webhook -> docker build -> docker run",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker",
    "git",
    "webhook",
    "deploy"
  ],
  "author": "Christopher Ayling",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.18.0",
    "express": "^4.16.3",
    "shelljs": "^0.8.2"
  }
}
